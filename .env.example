# ==================================
# 日志配置
# ==================================

# --- 日志级别 ---
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
# 建议: 开发环境使用 DEBUG，生产环境使用 INFO 或 WARNING
LOG_LEVEL=INFO

# --- 日志格式 ---
# 可选值: console (易读), detailed (详细), simple (简洁)
# 建议: 本地开发使用 console，生产环境使用 detailed
LOG_FORMAT=console

# ==================================
# 数据库配置
# ==================================

# --- 数据库配置 ---
# 示例: mysql://user:password@host:port/database
DATABASE_URL="your_mysql_connection_string"

# --- IMAP 邮箱配置 ---
IMAP_HOSTNAME="imap.example.com"
IMAP_USERNAME="user@example.com"
IMAP_PASSWORD="your_email_password"

# --- WebDAV 服务器配置 ---
# ⚠️ 重要提示：首次启动后，服务器配置会导入到数据库中，之后应该通过 /servers Web 界面管理
# 使用 JSON 数组格式配置一个或多个 WebDAV 服务器
# 首次启动时，所有配置的服务器会自动导入到数据库
#
# 单服务器示例:
# WEBDAV_SERVERS='[{"name":"Main","url":"https://webdav.example.com/dav","login":"username","password":"password"}]'
#
# 多服务器示例:
WEBDAV_SERVERS='[{"name":"Main","url":"https://webdav.example.com/dav","login":"user","password":"pass"},{"name":"Backup","url":"https://backup.example.com/dav","login":"user","password":"pass"}]'

# --- 邮件搜索配置 ---
EMAIL_SEARCH_SUBJECT="your_keyword"

# --- API 安全配置 ---
# 用于保护 /api/run-task 端点，请使用一个长而随机的字符串
API_SECRET_KEY="a_very_long_and_random_secret_key"

# 用于保护内部 worker 端点，防止外部直接调用
INTERNAL_API_KEY="another_super_long_and_random_secret_key"

# --- Web 页面认证配置 ---
WEB_AUTH_USER="admin"
WEB_AUTH_PASSWORD="a_secure_password"

# --- Flask Session 配置 ---
# Flask session 密钥，用于会话加密
FLASK_SECRET_KEY="your_flask_secret_key_here"

# --- 上传重试配置 (可选) ---
UPLOAD_RETRY_COUNT=3
UPLOAD_RETRY_DELAY=5

# --- 附件处理限制 (可选) ---
# 单个附件最大大小限制 (MB)，默认 50MB
MAX_ATTACHMENT_SIZE_MB=50

# 每次运行最多处理的邮件数量，默认 10
MAX_EMAILS_PER_RUN=10
